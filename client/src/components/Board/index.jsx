import React from 'react';
import Topic from '../../containers/Board/Topic';

const Board = () => <Topic />;
// the board component displays Topic and board title. the latter is in the Toolbar component  
export default Board;

